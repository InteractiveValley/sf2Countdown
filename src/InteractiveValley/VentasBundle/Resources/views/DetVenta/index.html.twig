{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DetVenta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cantidad</th>
                <th>Precio</th>
                <th>Iva</th>
                <th>Importe</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('detventas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cantidad }}</td>
                <td>{{ entity.precio }}</td>
                <td>{{ entity.iva }}</td>
                <td>{{ entity.importe }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('detventas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('detventas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('detventas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
